import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    margin: 4,
    paddingVertical: 8,
    paddingHorizontal: 12,
    borderRadius: 6,
    justifyContent: 'space-between',
    backgroundColor: '#8db4f9',
  },
});
export default styles;
